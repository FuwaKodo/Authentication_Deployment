# Authentication_Deployment
A simple full-stack authentication app built with React, Vite, and Express.js, featuring user registration, login, persistent session handling with JWT, CORS configuration, and deployment-ready frontend/backend setup.
